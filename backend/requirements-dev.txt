-r requirements.txt

# Test runner
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: EverOS Cloud SDK (enables full EverMem Cloud API tests).
# NOTE: the official Cloud SDK was renamed from `everos` → `everos-cloud`
# when the OSS local-first variant took the `everos` name on PyPI.
everos-cloud>=0.4.0
